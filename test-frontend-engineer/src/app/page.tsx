import MiniBar from "@/components/shared/MiniBar";
import SearchBar from "@/components/shared/SearchBar";

const page = () => {
  return (
    <>
      <MiniBar />
      <SearchBar />
    </>
  );
};

export default page;
